repo: agda
owner: agda
name: agda

paths:
  data_dir: data/agda
  report_path: reports/agda-opportunities.md

github:
  per_page: 100
  max_issues: 400
  max_prs: 300
  use_cache: true
  max_issue_context: 25

issue_queries:
  state: open